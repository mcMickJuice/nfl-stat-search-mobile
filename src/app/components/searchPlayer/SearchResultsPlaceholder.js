import React, {PropTypes} from 'react'
import {View, StyleSheet, Text} from 'react-native'

const SearchResultsPlaceholder = () => {
    return <View>
        <Text>
            No Results
        </Text>
    </View>
}

SearchResultsPlaceholder.propTypes = {}

export default SearchResultsPlaceholder